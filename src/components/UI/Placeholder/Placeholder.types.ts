export interface PlaceholderProps {
  /**
   * The size of the placeholder box
   */
  size: 'small' | 'medium' | 'large';
  /**
   * The width of the box, default: 100% (full available width)
   */
  width?: number | string;
}
