export interface TabWrapperProps {
  /**
   * The content of the tab, any (children) element will be rendered on the same row
   */
  children: React.ReactNode;
  /**
   * Class to personalise the styles
   */
  className?: string;
}
